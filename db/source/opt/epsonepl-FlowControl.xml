<option type="enum" id="opt/epsonepl-FlowControl">
  <!-- A multilingual <comments> block can appear here, too;
       it should be treated as documentation for the user. -->
  <arg_longname>
   <en>Flow Control</en>
  </arg_longname>
  <arg_shortname>
   <en>Flow Control</en><!-- backends only know <en> shortnames! -->
  </arg_shortname>
  <arg_execution>
   <arg_group>General</arg_group>
   <arg_order>200</arg_order>
   <arg_spot>B</arg_spot>
   <arg_substitution />
   <arg_proto>EplFlowControl=%s,</arg_proto>
  </arg_execution>
  <constraints>
     <constraint sense="true">
      <driver>epl5700l</driver>
      <arg_defval>ev/1000</arg_defval>
     </constraint>
<!--
     <constraint sense="true">
      <driver>epl5800l</driver>
      <arg_defval>ev/1000</arg_defval>
     </constraint>
     <constraint sense="true">
      <driver>epl5900l</driver>
      <arg_defval>ev/1000</arg_defval>
     </constraint>
-->
  </constraints>
  <enum_vals>
   <enum_val id="ev/1000">
    <ev_longname>
     <en>Off</en>
    </ev_longname>
    <!-- A multilingual <comments> block can appear here, too;
         it should be treated as documentation for the user. -->
    <ev_shortname>
     <en>Off</en>
     <!-- Until someone tells me how to learn the user locale in 
          backends, the shortname must be monolingual in <en>! -->
    </ev_shortname>
    <ev_driverval>off</ev_driverval>
   </enum_val>
   <enum_val id="ev/1001">
    <ev_longname>
     <en>USB (libusb)</en>
    </ev_longname>
    <!-- A multilingual <comments> block can appear here, too;
         it should be treated as documentation for the user. -->
    <ev_shortname>
     <en>libusb</en>
     <!-- Until someone tells me how to learn the user locale in 
          backends, the shortname must be monolingual in <en>! -->
    </ev_shortname>
    <ev_driverval>libusb</ev_driverval>
   </enum_val>
   <enum_val id="ev/1002">
    <ev_longname>
     <en>USB (Kernel /dev/usb/lp0)</en>
    </ev_longname>
    <!-- A multilingual <comments> block can appear here, too;
         it should be treated as documentation for the user. -->
    <ev_shortname>
     <en>kernelusblp0</en>
     <!-- Until someone tells me how to learn the user locale in 
          backends, the shortname must be monolingual in <en>! -->
    </ev_shortname>
    <ev_driverval>/dev/usb/lp0</ev_driverval>
   </enum_val>
   <enum_val id="ev/1003">
    <ev_longname>
     <en>USB (Kernel /dev/usb/lp1)</en>
    </ev_longname>
    <!-- A multilingual <comments> block can appear here, too;
         it should be treated as documentation for the user. -->
    <ev_shortname>
     <en>kernelusblp1</en>
     <!-- Until someone tells me how to learn the user locale in 
          backends, the shortname must be monolingual in <en>! -->
    </ev_shortname>
    <ev_driverval>/dev/usb/lp1</ev_driverval>
   </enum_val>
  </enum_vals>
</option>
