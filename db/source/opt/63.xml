<option type="enum" id="opt/63">
  <!-- A multilingual <comments> block can appear here, too;
       it should be treated as documentation for the user. -->
  <arg_longname>
   <en>Bidirectional printing</en>
  </arg_longname>
  <arg_shortname>
   <en>Bidirectional</en><!-- backends only know <en> shortnames! -->
  </arg_shortname>
  <arg_execution>
   <arg_order>135</arg_order>
   <arg_spot>B</arg_spot>
   <arg_substitution />
   <arg_proto>%s</arg_proto>
  </arg_execution>
  <constraints>
     <constraint sense="true">
      <driver>pnm2ppa</driver>
      <arg_defval>ev/pnm2ppa-Bidirectional-Default</arg_defval>
     </constraint>
  </constraints>
  <enum_vals>
   <enum_val id="ev/pnm2ppa-Bidirectional-Default">
    <ev_longname>
     <en>Default</en>
    </ev_longname>
    <!-- A multilingual <comments> block can appear here, too;
         it should be treated as documentation for the user. -->
    <ev_shortname>
     <en>Default</en>
     <!-- Until someone tells me how to learn the user locale in
          backends, the shortname must be monolingual in <en>! -->
    </ev_shortname>
    <ev_driverval></ev_driverval>
   </enum_val>
   <enum_val id="ev/pnm2ppa-Bidirectional-Off">
    <ev_longname>
     <en>Off (better quality)</en>
    </ev_longname>
    <!-- A multilingual <comments> block can appear here, too;
         it should be treated as documentation for the user. -->
    <ev_shortname>
     <en>Off</en>
     <!-- Until someone tells me how to learn the user locale in
          backends, the shortname must be monolingual in <en>! -->
    </ev_shortname>
    <ev_driverval> --uni</ev_driverval>
   </enum_val>
   <enum_val id="ev/pnm2ppa-Bidirectional-On">
    <ev_longname>
     <en>On (faster)</en>
    </ev_longname>
    <!-- A multilingual <comments> block can appear here, too;
         it should be treated as documentation for the user. -->
    <ev_shortname>
     <en>On</en>
     <!-- Until someone tells me how to learn the user locale in
          backends, the shortname must be monolingual in <en>! -->
    </ev_shortname>
    <ev_driverval> --bi</ev_driverval>
   </enum_val>
  </enum_vals>
</option>
